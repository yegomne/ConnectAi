# 의사결정 기록 (Decision Log)

> 미래의 사장님(3개월 뒤)이 "왜 이렇게 했더라" 할 때 답을 주는 문서.
> 각 결정에는 **상황 → 후보 → 선택 → 근거 → 검증**을 기록.

---

# 1단계: 상품명 리네이머 의사결정 (D-001 ~ D-013)

---

## D-001: 자동 이미지 마스킹 → 제외

**상황**: 공급사 이미지의 노출 부위(성기·유두) 자동 처리 방식 검토

**고려한 후보**
- A) NudeNet + Stable Diffusion 인페인팅
- B) 단순 모자이크/블러 처리
- C) 포토샵 수동 처리
- D) YOLOv8/SAM 직접 학습

**선택**: **C) 포토샵 수동 처리**

**근거**
- 사장님 포토샵 숙련도로 1장당 2분 내 완료
- 자동화 시 오탐 검수 시간 + 인페인팅 품질 검증이 2분보다 더 소요
- 단건 검수 워크플로우라 대량 처리 이점 없음

**관련 결정**: NSFW 스크리너(어떤 이미지가 포토샵 대상인지 분류)는 유지 → D-008

---

## D-002: LLM 모델 선정 — qwen3:14b 확정

**상황**: 한국어 SEO 카피라이팅용 로컬 LLM 선택. 환경 제약 RTX 5060 Ti 16GB

**고려한 후보**
- A) qwen3:8b (5.2GB)
- B) qwen3.6:27b (17GB) — 보유 중
- C) qwen3.6:35b-a3b (23GB) — 보유 중, MoE
- D) qwen3:14b (9GB) — 신규 다운로드
- E) exaone3.5:7.8b (4.8GB)
- F) gpt-oss:20b (12GB)

**선택**: **D) qwen3:14b**

**근거**
- B/C는 VRAM 초과 → eval rate 3 tok/s 측정 (정상 60+)
- A는 명령어 추종 부족 (글자수 제약 미준수)
- D는 100% GPU 로드 + 명령어 추종 우수, 응답 7~9초

**검증**: 6개 카테고리 × 5후보 = 30개 후보 100% 통과

**부산물**: B, C는 디스크 점유만 함 → 삭제 권장 (40GB 확보)

---

## D-003: thinking 모드 ON 유지

**상황**: qwen3 thinking 모드 제어 방식

**문제**: 초기 `/no_think` 지시자 사용 → 14b로 격상 후 모든 응답이 빈 JSON `{}`

**원인**
- qwen3 14b는 thinking 의존도가 8b보다 큼
- `/no_think` + `format=json` 조합이 모델 내부 충돌

**해결**
- 프롬프트에서 `/no_think` 제거
- Ollama API에 `"think": false` 정식 옵션 사용
- 빈 응답 시 자동 재시도 + thinking 켜고 폴백

**검증**: JSON 파싱 실패 0회, 응답 7~9초 안정

---

## D-004: 카테고리힌트 입력 방식 도입

**상황**: 정보 부족한 상품명 처리 (예: `L-828 A-T512`)

**선택**: 사장님이 카테고리힌트 한 줄 입력 (`상품명 | 힌트` 형식)

**근거**: LLM 추측은 환각 위험, 비전 모델은 4단계 범위 초과

**향후**: 4단계 비전 모델로 자동 생성

---

## D-005: 5가지 후보 전략의 카테고리별 변형

**문제**: "혼자 즐기는" 전략을 모든 카테고리에 적용 → 수갑·란제리에 부적절

**해결**: 5번 전략(용도/상황 강조)을 카테고리별로 분기
- 남성/여성/후방: "혼자 즐기는", "솔로"
- 속옷/란제리: "데이트 룩", "야간"
- 커플/수갑/안대: "커플 데이트", "특별한 밤"

---

## D-006: 자동 교정 후처리 도입

**상황**: 프롬프트로 카테고리 격리 규칙 명시했으나 LLM이 무시 (M-314 애널 → "명기" 5/5)

**해결**: 코드 단에서 강제 치환 (`CATEGORY_REPLACEMENT_MAP`)

**처리 순서**
1. LLM 원본 생성
2. **카테고리 오염 자동 교정** ← 핵심
3. 단어 중복 제거 (max 2회)
4. 글자수 확인
5. 필요시 부스터 추가

**철학**: LLM 80% + 후처리 20% = 100%. 둘 다 필요.

---

## D-007: 단어 중복 제거 — max 2회

**상황**: 자동 교정 + 부스터 추가 후 같은 단어 3회+ 등장

**선택**: max 2회 허용

**근거**
- max 1회: SEO 가중치 손해
- max 3회+: 스팸성으로 보일 위험
- max 2회: 균형

---

## D-008: NSFW 스크리너 vs 자동 마스킹

**선택**: llama3.2-vision으로 이미지 3단계 분류 (🟢🟡🔴)

**효과**: 🔴 폴더로 자동 분리 → 사장님이 그 폴더만 포토샵 작업

**구현 단계**: 4단계

---

## D-009: 마켓 자동 등록 → 보류

**선택**: 완전 수동 (당분간)

**근거**: 단건 검수 방침, 마켓 약관 위험, 셀렉터 유지보수 부담

**전환 조건**: 월 100건 이상 정기 등록 단계 도달 시 재검토

---

## D-010: 부스터 키워드 카테고리별 분기

```python
BOOSTER_MALE = ["어덜트굿즈", "19금용품", "남성전용", "프리미엄", "신상"]
BOOSTER_FEMALE = ["여성전용", "이너케어", "릴렉서", "프리미엄", "신상"]
BOOSTER_ANAL = ["후방케어", "백도어용품", "19금용품", "프리미엄", "신상"]
BOOSTER_LINGERIE = ["이너웨어", "섹시속옷", "19금속옷", "프리미엄", "신상"]
BOOSTER_COUPLE = ["커플용품", "롤플레이", "코스튬", "프리미엄", "신상"]
```

후보 인덱스(0~4)별로 순환 적용 → 5개 후보가 다른 부스터로 끝남

---

## D-011: 응답 시간 허용 기준 — 단건 10초 이내

**근거**: 단건 검수 워크플로우라 30초~1분 머무는 시간에 8초 자연 흡수

**모니터링**: 평균 15초 초과 시 모델 재선정

---

## D-012: 환경변수 관리 — .env 파일

`SECOND_PC_IP`, `OLLAMA_PORT`, `MODEL_NAME`, `REQUEST_TIMEOUT`

`.gitignore`에 `.env` 포함

---

## D-013: 검증의 다중 안전망

7단계 안전망:
1. 프롬프트 단계 (카테고리별 금지/허용)
2. LLM 생성 후 자동 교정
3. 단어 중복 제거
4. 글자수 검증
5. 부스터 자동 추가
6. 부스터 후 중복 재정리
7. 사장님 검수 (5개 중 선택)

**철학**: LLM도 사람도 단독으론 완벽하지 않음 → 여러 단계로 품질 보장

---

# 2단계: FastAPI 서버화 의사결정 (D-014 ~ D-019)

---

## D-014: 서버 구동 방식 — 단계별 전환

**선택**: A(수동 실행) → C(작업 스케줄러)

**근거**
- 개발 단계: 콘솔 로그 즉시 확인 가능
- 운영 단계: 부팅 자동 시작

**전환 조건**: 3·4단계 완성 후 1주일 무사고 운영 시점

---

## D-015: 코드 위치 — 기존 프로젝트 내부 통합

**선택**: `adult_product_renamer/src/server.py`로 추가

**근거**: 1단계 모듈을 `from src.renamer import Renamer`로 그대로 import

---

## D-016: 메인↔세컨 동기화 — SMB 네트워크 드라이브

**구축**
- 세컨: `New-SmbShare -Name "renamer" -Path "C:\Users\pthem\Documents\adult_product_renamer" -FullAccess "yegom\pthem"`
- 메인: `New-PSDrive -Name "Z" -PSProvider FileSystem -Root "\\192.168.0.200\renamer" -Credential "yegom\pthem" -Persist`

**주의**: venv는 PC 종속이라 동기화 X (`robocopy /XD .venv`)

---

## D-017: 인증 — 없음 (내부망 전용)

**근거**: 외부 인터넷 노출 안 함, 방화벽 8000 외부 차단

**전환 조건**: 모바일 외부 접속 필요 시 → API 키

---

## D-018: FastAPI의 진짜 부가 가치 — 모델 로딩 비용 제거

**실측**
```
전체 8초 중:
├── HTTP 통신: 0.002초
├── Ollama 큐: 0.1초
└── LLM 추론: 7.9초 (99.97%)
```

**핵심 가치**
- CLI: 매 실행 5~10초 워밍업 + 추론 = 13~18초
- FastAPI: 서버 시작 시 1회 워밍업 → 매 요청 8초

---

## D-019: 회귀 테스트 의무화 + PowerShell 한글 깨짐 우회

**도구**: `tests/test_api.py` (1단계와 동일한 6 샘플)

**환경변수 지원**: `API_BASE_URL`로 메인/세컨 양쪽 실행 가능

### 부산물: PowerShell `Invoke-RestMethod` 한글 깨짐

**원인**: 응답 디코딩 시 인코딩 추측 실패 → 이미 깨진 데이터 복구 불가

**해결 3종**
1. Swagger UI 사용 (가장 쉬움)
2. `Invoke-WebRequest` + `[System.Text.Encoding]::UTF8.GetString()` 수동
3. Python 스크립트 (`sys.stdout.reconfigure(encoding='utf-8')`)

**채택**: 운영은 Python 자동화, 개발 검증은 Swagger UI

---

# 3단계: Streamlit GUI 의사결정 (D-020 ~ D-029)

---

## D-020: GUI 프레임워크 — Streamlit

**상황**: 단건 검수 GUI 구축

**고려한 후보**
- A) Streamlit
- B) PyQt6
- C) Tauri/Electron + React

**선택**: **A) Streamlit**

**근거**
- 단건 검수 워크플로우(상품 → 후보 → 선택 → 다음)에 완벽 매칭
- 같은 기능을 50~100줄로 구현 (PyQt 대비 1/3)
- 4단계에서 이미지 표시·통합 쉬움
- 사장님이 이미 웹 UI(Claude, Gemini) 익숙

**단점 수용**: 데스크탑 앱 느낌 아님 → 로컬 전용이라 사장님만 쓰시면 OK

---

## D-021: GUI 실행 위치 — 메인 PC

**선택**: 메인 PC에서 `streamlit run`, 브라우저는 `localhost:8501`

**근거**
- 사장님 작업 흐름이 메인 PC 기반
- 메인 PC GUI ↔ 세컨 PC FastAPI(8000) HTTP 호출
- 메인 PC는 가볍게 (LLM은 세컨 PC 전담)

---

## D-022: 환각 처리 — 6번째 카드 + 강화학습 (방향 C)

**상황**: LLM이 제품 이미지를 못 봐서 "강력진동" 같은 실제와 다른 키워드 생성 위험

**고려한 후보**
- 방향 A: 비전 모델로 자동 추출 (4단계로 이전)
- 방향 B: 현재 방식 유지 + 6번째 카드 + 강화학습
- 방향 C: B 먼저 → 나중에 A 통합

**선택**: **C** (B를 먼저, A는 4단계)

**근거 (사장님 의견)**
- 사장님이 키워드 직접 클릭하면서 데이터가 누적됨
- 1~2개월 후 강화학습 데이터로 5개 후보 자체가 사장님 스타일로 진화
- 4단계 비전 모델 통합 시 사장님은 확인·수정만

**구조**
```
[Day 1]   LLM 5후보 → 사장님 6번째 카드 직접 조립 → 키워드 저장
[Day 30]  같은 카테고리 재방문 → 이전에 사용한 키워드 자동 표시 → 클릭만
[Day 90]  강화학습 데이터 → LLM 프롬프트에 자동 주입 → 환각 자연 감소
```

---

## D-023: 카테고리 입력 UI — 드롭다운 추가

**상황**: 강화학습 데이터를 어느 풀에 저장할지

**선택**: 대분류 + 소분류 드롭다운 추가 (자동 키워드 주입은 안 함)

**근거**
- 사장님이 옵션 B(나중에 강화) 선택 → 자동 입력 X
- 카테고리는 강화학습 **분류 키**로만 사용
- 미선택 시 "미분류>전체"로 자동 저장
- 27개 소분류 분리 안 하면 "콘돔"과 "수갑" 키워드가 한 풀에 섞임

**카테고리 마스터** (리보스 사이트 실제 트리)
- 7개 대분류 × 27개 소분류
- `gui/categories.py`의 `CATEGORY_TREE` 딕셔너리에 저장

---

## D-024: 카테고리 사전 — 옵션 B (운영 중 학습)

**상황**: 27개 소분류별 키워드/금칙어 사전 작성 방법

**고려한 후보**
- A) 사장님이 카테고리별로 직접 입력 (사전 작성)
- B) GUI 만들고 운영하면서 자연 학습
- C) Claude가 일반 도매 SEO 관행으로 초안

**선택**: **B**

**근거 (사장님 의견)**
- "제품마다 특징이 있어서 제품을 보면서 해야 함"
- 미리 만든 사전은 제품 실물 모르고 작성한 추측
- 사용 중 입력한 키워드가 그대로 사전이 됨 → 100% 정확

**효과**: 2~4주 운영 후 사장님 전용 사전 자동 완성

---

## D-025: 6번째 카드 키워드 분류 — 옵션 C + B 보조

**상황**: 5개 후보 키워드를 어떻게 표시할 것인가

**고려한 후보**
- A) 단순 중복 제거 후 칩 표시
- B) 빈도순 정렬 (자주 나온 키워드 먼저)
- C) 카테고리별 분류 (핵심/카테고리/스펙/형용사/부스터/용도)

**선택**: **C 메인 + B 보조** (B는 접힌 상태로 아래에)

**분류 로직** (`gui/keyword_extractor.py`)
```python
def classify_keyword(word: str, original_name: str) -> str:
    if word in original_name: return "core"        # 원본 유래
    if word in BOOSTER_KEYWORDS: return "booster"
    if word in USAGE_KEYWORDS: return "usage"
    if word in ADJECTIVE_KEYWORDS: return "adjective"
    if re.search(r'\d+', word): return "spec"      # 숫자 포함
    if word in SPEC_KEYWORDS: return "spec"
    if word in CATEGORY_KEYWORDS: return "category"
    if word.endswith(("한", "운", "는")): return "adjective"
    return "category"  # 기본값
```

**근거**
- C: 사장님이 "어떤 종류 키워드인지" 빠르게 판단 가능
- B 보조: 빈도 정보도 가끔 필요 (자주 나오는 게 핵심일 수 있음)

---

## D-026: 빼야 할 키워드 처리 — 옵션 A (즉시 토글)

**상황**: 사장님이 "들어가선 안될 것들이 보여"

**고려한 후보**
- A) 클릭 토글 (이번엔 안 씀)
- B) 영구 블랙리스트 (앞으로 안 보이게)
- C) A + B

**선택**: **A**

**근거**: 일단 가볍게. 자주 거부하는 키워드 패턴 보이면 B 추가

**향후**: SQLite `category_keywords.is_blocked` 컬럼은 미리 만들어둠

---

## D-027: 6번째 카드 입력 방식 — 옵션 B (클릭 + 자유 편집)

**선택**: 키워드 클릭으로 추가 + 텍스트박스에서 자유 편집·삭제·순서 변경 가능

**근거**
- 빠른 조립: 칩 클릭
- 미세 조정: 직접 타이핑
- 자유도 최대

**Streamlit 제한**: `text_input`은 실시간 갱신 안 됨 (D-028 참조)

---

## D-028: 글자수 제한 — 50자 고정 + 실시간 갱신 보류

**선택**: 50자 고정 (모든 마켓 호환)

**근거**: 결정 ②(마켓 구분 X)와 일관성

### 부산물: 실시간 글자수 갱신 보류

**상황**: 직접 타이핑 시 글자수 카운터가 즉시 갱신 안 됨

**원인**: Streamlit `text_input` 위젯의 기본 동작 (blur 시점에만 서버 전달)

**고려한 후보**
- A) 안내 문구만 표시 ("Enter 또는 다른 곳 클릭 시 갱신")
- B) `text_area`로 변경 (약간 더 즉각적)
- C) HTML/JS 커스텀 컴포넌트 (진짜 실시간)
- D) 그냥 진행

**선택**: **A** (이미 안내 표시됨)

**근거**
- 키워드 칩 클릭은 실시간 작동 (사장님 주 작업)
- 직접 타이핑만 지연 (보조 작업)
- 결정 버튼 누르기 직전 blur 시점에 자동 갱신 + 50자 초과 시 비활성화 → 안전망 작동
- 방안 C는 ROI 낮음 (30분 작업 vs 페이즈 3 진행)

---

## D-029: 6번째 카드 키워드 칩 — 순수 토글 방식

**상황**: 키워드 칩 클릭 시 무조건 추가만 되어 같은 칩 다시 눌러도 중복 추가됨

**고려한 후보**
- A) 순수 토글 (클릭=추가, 다시 클릭=제거)
- B) 추가 전용 + 별도 되돌리기 버튼

**선택**: **A) 순수 토글**

**근거 (사장님 의견)**
- "잘못 선택했을 때 다시 누르면 없어져야 하는데 계속 추가됨"
- 사장님 표현이 정확히 토글 동작
- 같은 키워드 2번 넣기는 직접 편집 텍스트박스로 처리

**구현 핵심**
- `keywords` 리스트를 **단일 진실 소스(SSOT)**
- 텍스트는 항상 `" ".join(keywords)`로 재생성 (문자열 replace 안 씀 → 부분일치 오류 방지)
- 선택된 칩: `✓ {word}` + `type="primary"` 시각 강조

```python
def _on_keyword_toggle(keyword, kw_state_key, text_state_key):
    keywords = st.session_state.get(kw_state_key, [])
    if keyword in keywords:
        keywords = [k for k in keywords if k != keyword]  # 제거
    else:
        keywords = keywords + [keyword]                    # 추가
    st.session_state[kw_state_key] = keywords
    st.session_state[text_state_key] = " ".join(keywords)
```

**검증**: 사장님 직접 확인 — "아직 이상 안 보임"

---

## 부록 A: 단계별 통계

| 단계 | 작업 시간 | 주요 산출물 | 검증 결과 |
|---|---|---|---|
| 1단계 | 8 사이클 | renamer.py, validator.py | 30/30 통과 |
| 2단계 | 4 사이클 | server.py, test_api.py | 30/30 회귀 통과 |
| 3단계 페이즈 0~2.5 | 4 사이클 | gui/app.py 외 6개 | 메인 화면 정상 (텍스트 실시간만 제한) |
| 3단계 페이즈 3 | 1 사이클 (상위 모델) | 이력/통계 페이지, excel_export | 이력·재선택·차단·엑셀·통계 정상 |
| 3단계 페이즈 3.5 | 1 사이클 | components.py 토글 | 키워드 토글 정상 |

## 부록 B: 의존성 누적 (`requirements.txt`)

```
# 1단계
requests>=2.31.0
python-dotenv>=1.0.0
pytest>=7.4.0

# 2단계
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# 3단계
streamlit>=1.32.0
openpyxl>=3.1.0
pandas>=2.0.0
```

## 부록 C: 학습한 함정 통합 리스트

1. **qwen3 thinking 함정**: `/no_think` + JSON = 빈 응답 (D-003)
2. **VRAM 초과 함정**: 80% 이상 → 추론 속도 20배 저하 (D-002)
3. **카테고리 격리 함정**: 프롬프트만으론 불가 → 후처리 필수 (D-006)
4. **PowerShell 한글 함정**: `Invoke-RestMethod` 디코딩 실패 (D-019)
5. **venv 동기화 함정**: PC 종속, 별도 생성 (D-016)
6. **Streamlit 버튼 함정**: `st.columns` + `st.rerun()` 클릭 신호 소실 → on_click 콜백 사용 (페이즈 2.5)
7. **Streamlit text_input 함정**: 실시간 갱신 불가 (D-028)
8. **키워드 토글 SSOT 함정**: 문자열 replace로 키워드 제거 시 부분일치 오류 → keywords 리스트를 단일 진실 소스로 두고 텍스트 재생성 (D-029)
